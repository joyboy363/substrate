ADMIN_PASSWORD=
ADMIN_SECRET_SLUG=

# Create an Upstash Redis database at vercel.com -> Storage -> Create Database -> Upstash,
# or directly at upstash.com, then copy its REST URL/token here (or run `vercel env pull .env.local`).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
